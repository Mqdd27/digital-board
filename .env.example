# Where the SQLite file lives. Defaults to ./db/board.sqlite.
# Point this at a mounted volume when running in Docker.
# DATABASE_PATH=/data/board.sqlite

# Next.js listens on 3000 by default.
# PORT=3000
